# PEOS native adapter: setup

Locate the umbrella root and inspect top-level repositories, local instruction files, build manifests, infrastructure directories, and existing documentation. Do not scan generated dependency/build directories or read secret files.

If `peos/` is absent and writes are authorized, reproduce `defaults/project/peos/`, replacing the organization marker and preserving all sensible defaults. Then add evidence-based repository entries and validation commands. If it exists, audit it against `contracts/workspace.md` without overwriting client decisions.

Report detected repositories, unresolved ownership/contracts, disabled integrations, and the next recommended `/guide` request.
